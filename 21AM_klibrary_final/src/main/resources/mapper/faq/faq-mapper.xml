<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="faq">
	
	<!-- FAQ 전체 불러오기  -->
	<select id="selectFaqList" resultType="map">
		SELECT * FROM FAQ
	</select> 
	
	<!-- 게시글 전체 목록 조회 및  검색 조회  -->
	<!-- <select id="listAll" resultType="com.kh.klibrary.faq.model.vo.Faq">
		SELECT FAQ_NO, FAQ_CATE, FAQ_CONTENT, FAQ_ANSWER
		FROM FAQ
		WHERE 절을 include 태그로 삽입
		<include refid="search"></include>
		OREDER BY FAQ_NO DESC
	</select> -->
	
	<!-- 게시글 레코드 갯수  -->
<!-- 	<select id="countArticle" resultType="int">
		SELECT COUNT (*) FROM FAQ
		WHERE 절을 include 태그로 삽입
		<include refid="search"></include>
	</select> -->
	
	<!-- sql code 조각  -->
	<!-- 반복되는 sql의 일부를 sql 태그를 이용하여 따로 빼둘 수 있다  -->
	<!-- <sql id="search">
		<choose>
			검색옵션이 전체 검색일 경우
			<when test="searchOption == 'all'">
				WHERE FAQ_CATE LIKE '%'||#{keyword}||'%'
				OR FAQ_CONTENT LIKE '%'||#{keyword}||'%'
			</when>
			전체 검색이 아닐 경우 
			<otherwise>
				WHERE ${searchOption} LIKE '%'||#{keyword}||'%'
			</otherwise>
		</choose>
	</sql> -->
	
</mapper>
